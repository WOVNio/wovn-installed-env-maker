version: '2.1'
services:
  app:
    build:
      context: .
      args:
        RUBY_VERSION: 2.7
    image: wovnrb-app:ruby-${RUBY_VERSION:-2.7}
    ports:
      - 8080:4567
    working_dir: /app
    volumes:
      - ./app:/app

